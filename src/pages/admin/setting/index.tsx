import React from 'react';
import withTokenAuth from '../../../hoc/withTokenAuth';
import TeamSetting from '../../../components/organisms/TeamSetting';

const Setting = () => {
  return <TeamSetting />;
};

export default Setting;
